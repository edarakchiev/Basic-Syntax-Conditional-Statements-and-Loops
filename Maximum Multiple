divisor = int(input())
bound = int(input())
max_num = 0
for num in range(1, bound+1):
    if num % divisor == 0 and bound >= num > 0:
        if num > max_num:
            max_num = num
print(max_num)
